# Plant Engine v0.6.0

iPadを主対象とした、植物相談用のPWAプロトタイプです。チャット型AIではなく、現地カルテ・植物データベース・条件照合・組み合わせ検証を行う専門家用ワークベンチとして設計しています。

## 植物データ

- 確認済みコア: 16件
- STEP 5 候補母集団: 180件（暫定値・地域適合未検証）
- 端末DB: Web調査結果を利用者が追加・更新

候補母集団は調査対象を広く拾うためのレコードです。推薦を確定する前に、分類・成熟寸法・気候適合・リスクを確認してください。植物本体の情報と地域評価は別項目として保持しています。

## 現在できること

- 現地写真の取り込み
- 地域、寸法、日照、土壌水分、高さ上限の入力
- 葉色・葉形・季節性・役割による植物候補の抽出
- 適合度と除外理由の表示
- 採用候補と株数の管理
- 成長後の概算占有量の計算
- 冬の構成、葉形、三層構成、繁殖性の検証
- 相談案件の端末内保存
- 写真をGitHubへ送らず、iPad内の写真用データベースに保存
- 相談メモのコピー
- PWAとしてホーム画面への追加とオフライン起動

## v0.1.1

- 「求める役割」の選択と解除を繰り返すと操作処理が重複し、動作が停止する問題を修正しました。

## v0.1.2

- iPadに旧版が残る場合でも新版へ切り替わるよう、PWAの更新処理とキャッシュ識別子を改善しました。

## v0.1.3

- iPad Safariで全解除後に役割ボタンが反応しない問題を避けるため、各ボタンへ操作を直接登録する方式へ変更しました。

## v0.1.4

- 既存植物を案件ごとに削除でき、削除状態を端末内へ保存できるようにしました。

## v0.2.0

- 条件外候補を赤い判定表示、低彩度、斜線背景で明確に区別しました。
- 候補カードへ株張り、冬姿、広がり方、葉色、役割一致数、確認点を追加しました。

## v0.3.0

- 現場で未知植物を調べるためのWeb調査プロンプト生成機能を追加しました。
- 調査結果JSONの構造検証、端末内植物DBへの登録、候補抽出への即時反映に対応しました。
- ユーザー登録植物の出典保持、更新、削除に対応しました。

## v0.3.1

- 未知植物の入口を候補検索直下の大きなボタンへ移動し、iPadでも見つけやすくしました。
- Service Workerの登録版を同期し、旧版キャッシュが残る問題を修正しました。

## v0.3.2

- AIがJSONの引用符を `“ ”` に変換した場合でも、自動修復して登録できるようにしました。
- ノーブレークスペースや不可視文字も登録前に除去します。

## v0.4.0

- 調査候補180件を候補DBとして追加し、合計196件から抽出できるようにしました。
- 確認済み・候補DB・端末DBの絞り込みを追加しました。
- 未検証の候補は「仮適合」「候補DB・暫定」と表示し、確定データと区別します。
- iPadの負荷を抑えるため、候補カードを60件ずつ表示します。

## v0.5.0

- 採用植物、株数、現地寸法、既存植物から、5種類の外観イメージ生成プロンプトを作成します。
- 5案は植物の種類を変えず、自然群生・リズム・立体レイヤー・焦点と余白・密度の違いで比較します。
- AIで得た画像を最大5枚まで案件へ貼り付け、端末内の画像DBへ保存できます。
- 保存画像は最大辺1600pxへ圧縮し、GitHubや外部サーバーには送信しません。

## v0.6.0

- 既存植物を追加し、現地写真をタップして各植物の固定位置を記録できるようにしました。
- 外観案プロンプトは既存植物を移植・削除・複製せず、その間の空地へ新規植物を配置するよう指定します。
- 植え付け予定日を基準に、1・2・3・6か月後と1年後の成長予測プロンプトを生成します。
- 成長系列ではカメラ・配置・株数を固定し、活着、株幅、季節色、開花、落葉・休眠だけを変化させます。
- 5時点の予測画像を外観案とは別に端末内へ保存できます。

## 起動

このフォルダで静的Webサーバーを起動してください。

```bash
python3 -m http.server 8080
```

Macでは `http://localhost:8080` を開きます。

## GitHub Pages

このフォルダをGitHubリポジトリの `main` ブランチへ置くと、同梱のワークフローがGitHub Pagesへ自動公開します。リポジトリの Settings → Pages → Source で **GitHub Actions** を選択してください。

公開されるのはHTML、CSS、JavaScript、植物データだけです。相談者の写真や相談内容はGitHubへ送信されず、利用中のiPad内に保存されます。

## 次期候補

- 相談案件一覧と複数案件保存
- 写真上への植栽範囲・寸法記入
- 上面配置ボード
- 1年後／3年後の成長表示
- 植物データの追加・編集・CSV読み込み
- PDF相談シート出力
- 植物画像と出典管理
